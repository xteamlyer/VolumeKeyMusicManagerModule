pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        jcenter()
        mavenCentral()
        maven {
            url "https://raw.githubusercontent.com/rovo89/XposedBridge/gh-pages/"
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        jcenter()
        mavenCentral()
        maven {
            url "https://raw.githubusercontent.com/rovo89/XposedBridge/gh-pages/"
        }
    }
}
rootProject.name = "VolumeKeyMusicManagerModule"
include ':app'